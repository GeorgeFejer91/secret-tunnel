[package]
name = "secret-tunnel"
version = "0.1.0"
description = "Minimal desktop launcher for a local Secret Tunnel MCP share."
authors = ["George Fejer"]
edition = "2021"
license = "MIT"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "chatgpt_local_mcp_launcher_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[features]
# Off by default: the online installer helper is unfinished and does not compile.
online-installer = []

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri-plugin-clipboard-manager = "2.3.3"
rfd = "0.17.2"
directories = "6.0.0"
uuid = { version = "1.26.1", features = ["v4"] }
fs4 = "0.13.1"
sha2 = "0.11.0"


# Read the optimization guideline for more details: https://tauri.app/concept/size/#cargo-configuration
[profile.release]
codegen-units = 1
lto = true
opt-level = 3
panic = "abort"
strip = true

[target.'cfg(any(target_os = "macos", windows, target_os = "linux"))'.dependencies]
tauri-plugin-autostart = "2.5.1"
