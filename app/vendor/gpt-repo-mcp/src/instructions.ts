import { MUTATING_TOOL_NAMES } from "./tools/mutating-tools.js";

const SIDE_EFFECTING_TOOLS = MUTATING_TOOL_NAMES.join(", ");

export const SERVER_INSTRUCTIONS = [
  "GPT Repo MCP is a read-mostly local repository app. Tools with local side effects require the relevant repository write or operations policy, or an explicitly configured provider capability, and use the MCP host approval boundary according to their annotations.",
  "GITHUB WORKFLOW. Local file editing, generic local Git operations, desktop-managed GitHub publishing and direct GitHub API publishing are separate capabilities. A refusal from one does not establish that the others are unavailable.",
  "For a GitHub task, inspect the currently exposed tools and github_runtime_status, using the host's tool-discovery mechanism when needed. Use github_status for the managed route, github_direct_status for the direct route, and repo_policy_explain when local policy is unclear. Tool visibility and account scopes are evidence, not a guarantee that a particular repository or protected branch permits the requested write. Edit allowed local files using repo_write_file or repo_write_changes.",
  "MANAGED: github_status, verify the intended binding and branch, then repo_ship with exact reviewed paths and a message. This creates a local commit and pushes it to the bound destination. When approvalMode is autonomous no further Secret Tunnel desktop approval click is needed; when it is local_approval the plan stays pending until a human approves it in the desktop, and no tool call grants that approval.",
  "DIRECT: github_direct_status, verify the intended repository and branch, then github_direct_publish with repository, explicit branch, exact reviewed paths and message, then github_direct_ref to verify. This creates a commit on GitHub from selected local files. It does not create a local Git commit or advance the local branch, and it does not require a desktop repository binding. Its own authorization and safety checks apply.",
  "Repository creation and Pages setup are separate optional steps, not prerequisites for editing files in a repository that already exists: github_repo_ensure, or github_create_repository then github_create_repository_apply, github_repo_rebind to move an existing binding deliberately, and github_pages_ensure then github_pages_status on the managed route; github_direct_repo_ensure, github_direct_pages_ensure and github_direct_pages_status on the direct route.",
  "OPERATIONS_DISABLED concerns the operation that returned it; it does not automatically disable file editing or the separate GitHub publishing tools. not_bound means the managed publishing destination is not configured, not that GitHub authorization is missing. HTTP 502 means the connection failed, not that write permission is absent.",
  "Use publication tools only for user-authorized publication, never merely to test permissions. Do not silently retarget a binding, assume the branch is main, switch publishing routes, or retry an uncertain mutation: reconcile the receipt and the remote state first with github_operation_status or github_direct_ref. Report the exact failing tool, code and sanitized message, and distinguish unavailable, unverified, policy-blocked and destination-not-configured.",
  `Tools with local side effects are ${SIDE_EFFECTING_TOOLS}. repo_code_index is a non-destructive idempotent provider/index mutation. repo_prepare_patchset is a non-destructive non-idempotent local metadata mutation. The remaining tools use their documented repository or local-state mutation contracts.`,
  "All access is scoped by repo_id and repo-relative POSIX paths. Secret blocking, default excludes, symlink containment, nested-repository boundaries, write policy, operation policy, stale-state guards, and structured validation are enforced by the server. Use repo_policy_explain instead of guessing when access is blocked.",
  "LOCAL DEVELOPMENT, local repository only. This path ends at a local commit and is not a prerequisite for either GitHub publishing route: repo_project_brief when product or repository context is needed; repo_current_work_session when resuming active or blocked work; repo_search followed by targeted repo_fetch_file or bounded repo_read_many; repo_context_map or repo_symbol_context only when impact evidence is needed; repo_write_file or repo_write_changes; repo_validate; repo_ship_review for combined readiness or repo_git_review for Git and recovery planning; then the exact canonical repo_write_stage_commit, repo_write_commit, or repo_write_recover payload returned by review. Use repo_git_diff with only repo_id on the first raw-diff call. repo_write_stage_commit is never required before repo_ship or github_direct_publish.",
  "Availability, authorization and successful execution are three different things. A tool present in your tool list can be called, but being listed does not prove that this repository, branch, account or policy permits the action, and a successful call is the only evidence that it did. Do not report a listed tool as unavailable without having called it, and do not treat one tool's refusal as evidence about another tool. When a call is refused, quote the returned code and message verbatim: a refusal is a policy or state answer from the server, not a missing action.",
  "The user and ChatGPT choose what to build. repo_project_brief supplies repository-owned product truth but never selects work. Use repo_change_plan only after the goal is chosen. Use repo_task_inventory only for explicit backlog discovery and repo_decision_memory only as supporting historical evidence. When product_brief is missing or invalid, continue with technical analysis only and do not infer product facts from key_docs, package metadata, decision memory, task inventory, or chat history. Canonical evidence paths are references for bounded follow-up reads.",
  "Direct implementation is the default. Do not insert task inventory, decision memory, patchsets, delegation, semantic review, or granular Git tools unless the request specifically needs them. Use patchsets only when atomic create, modify, edit, delete, rename, or first-class rollback is worth the extra prepare/apply/review steps. Use granular stage, unstage, restore, cleanup, or commit tools only when requested, troubleshooting, or a composite payload is unavailable. Dry-run is optional preview, not a required ritual. Omit optional reason by default. repo_write_commit and repo_write_stage_commit create local commits only; do not push and do not run shell commands.",
  "For local-only ChatGPT handoff requests such as skapa handoff, create handoff, skriv handoff, session handoff, resume note, fortsättningsanteckning, ny chatt context, or överlämning till nästa chatt, run repo_git_status and use repo_write_handoff rather than generic writers. Handoffs remain local unless the user explicitly requests public documentation.",
  "Use repo_start_work_session and repo_update_work_session for optional content-free implementation continuity. continuity_state distinguishes active work, blocked ongoing work, and completed_history. A completed current pointer returns compact metadata without the full session or historical next_action; pass work_session_id for full history. Work sessions do not choose the next goal and are distinct from human-readable handoffs.",
  "Use delegation tools only when the user explicitly requests Codex or implementation-agent delegation. Before product delegation, use repo_project_brief. New task creation is Delegation v3 only: include beneficiary, current problem, desired outcome, why now, kind-specific context, Starting points are not exhaustive, authorization scope, hard constraints, preservation rules, exclusions, and separate PAC/TAC criteria. Manual and queued v3 handoff are supported, but writing a task never starts a runner. Historical v1/v2 runs remain reviewable only.",
  "Runner completion never constitutes a final product verdict. Use repo_agent_runs for lifecycle and structured questions, repo_codex_review for deterministic integrity/scope/TAC/PAC evidence, and repo_write_codex_review for the state-bound qualitative product attestation. Agent PAC claims are evidence only. After a valid attestation, use the complete repo_ship_review payload. Corrective and scope-amendment children must use the complete review-provided task payload, preserve root contracts, and remain within the two-child limit. Advisory gates warn; enforce gates fail closed. Runner auto-commit and auto-push are disabled.",
  "repo_git_review and repo_ship_review default to compact output with canonical actual composite payloads. Request detail=full only for granular dry-run payloads or expert diagnostics. Validation, semantic findings, Git evidence, delegation gates, and recovery eligibility remain authoritative in both modes.",
  "Use repo_validate only with allowlisted test, build, lint, typecheck, smoke, or all profiles. Use repo_failure_diagnose for failed validation evidence, repo_semantic_review for standalone semantic risk analysis, and repo_code_index action=start only after explicitly ask the user for indexing approval."
].join(" ");
